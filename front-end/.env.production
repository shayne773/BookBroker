# Production builds call the API on the site's own origin: Vercel serves the
# Express API from back-end/ under /api (see vercel.json at the repository root).
# Overridden by VITE_SERVER_ADDRESS in the environment or in .env.production.local.
VITE_SERVER_ADDRESS=/api
